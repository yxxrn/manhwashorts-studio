# Historical documentation

Verbose pre-refactor `AGENTS.md` and `docs/STATUS.md` logs are intentionally not
duplicated in the current tree. They remain recoverable from Git history; the last
pre-documentation-sync refactor baseline is commit `4749633`.

Use historical commits only for incident/benchmark forensics. Do not copy old test
paths, machine-specific commands, gate behavior, or "next action" instructions back
into current docs without verifying them against current `main`.

Current authority: repository `AGENTS.md`, `docs/STATUS.md`,
`docs/ARCHITECTURE.md`, and `docs/MAINTAINER_GUIDE.md`.
